/*
1. Declare a variable called 'isIsland' and set its value according to your
country. The variable should hold a Boolean value. Also declare a variable
'language', but don't assign it any value yet
2. Log the types of 'isIsland', 'population', 'country' and 'language'
to the console.
*/

let isIsland = false;
let population = 3658974;
let country = "Brazil"
let language;
let nullValue = null;

console.log(typeof(isIsland)); //Return a boolean type
console.log(typeof(population)); //Returns a number type
console.log(typeof(country));  //Return a string type
console.log(typeof(language)); //Return an undefined type
console.log(typeof(nullValue)); // Error in JavaSript. It returns an object type instead of null