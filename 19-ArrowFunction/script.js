/*
1. Recreate the last assignment, but this time create an arrow function called
'percentageOfWorld3'
*/

"use strict"

//Arrow Function

const percentageOfWorld = percentageOfWorld3 => `${(population / 7900) * 100}%`;

const population = 2568;
console.log(percentageOfWorld (population));